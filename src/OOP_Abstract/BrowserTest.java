package OOP_Abstract;
public class BrowserTest {
	public static void main(String[] args) {
		Web wb = new Web();
		// web--default
		Browser br = new Browser();
		// web--default
		// Browser--default
		Browser cr = new Browser(10);
		// web--default
		// Browser--one param
}
}